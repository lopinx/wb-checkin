# WorkBuddy 签到 —— 本地运行环境变量模板
# 复制为 .env 后填写；.env 已在 .gitignore 中，不会被提交。
#
# 三种运行方式的环境变量名完全一致：
# - 本地 Node.js：读取 .env 文件（本文件复制改写）
# - GitHub Actions：在仓库 Settings → Secrets 配置，不使用 .env
# - Cloudflare Workers：用 wrangler secret put 配置

# 多账号（推荐）：每行一个账号，格式 ACCESS_TOKEN#UID#备注，换行分隔
# 例：
# WORKBUDDY="eyJxxx.aaa.bbb#u_123456#主号
# eyJyyy.ccc.ddd#u_654321#小号A"

# 单账号兜底（未设置 WORKBUDDY 时生效）
# WORKBUDDY_ACCESS_TOKEN=eyJxxx.aaa.bbb
# WORKBUDDY_UID=u_123456

# Cloudflare HTTP 手动触发鉴权（仅 CF 端本地调试 .dev.vars 用）
# WORKBUDDY_SECRET=

# 可选：WxPusher 通知
# WXPUSHER_APP_TOKEN=
# WXPUSHER_UID=